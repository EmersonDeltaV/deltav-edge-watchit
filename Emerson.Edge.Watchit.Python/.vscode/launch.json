{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "args": [
                "--provider-prefix", "PROPLUS_PREFIX",
                "--path", "example_path",
                "--edgeip", "localhost",
                "--username", "admin",
                "--password", "iamsecret"
            ]
        }
    ]
}